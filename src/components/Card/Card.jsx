const Card = ({
  children,
  className = "",
  variant = "default",
  hover = true,
  padding = "normal",
}) => {

  const baseStyles=""

  return <div></div>;
};
export default Card;

// <Card hover={false}  className="my-custom-class" variant="primary" padding="small"></Card>
